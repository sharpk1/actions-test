module.exports = {
    preset: 'ts-jest',
    testEnvironment: 'jsdom',
    globals: {
      // Define any global variables that your app uses in tests
    },
    // Add any other configuration options as needed
  };
  